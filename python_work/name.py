name = 'ada lovelace'

print(name.title())  # преобразует первый символ каждого слова в строке к верхнему регистру
print(name.upper())  # преобразует все символы каждого слова в строке к верхнему регистру
print(name.lower())  # преобразует все символы каждого слова в строке к нижнему регистру
